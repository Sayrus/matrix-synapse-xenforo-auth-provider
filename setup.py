from setuptools import setup

setup(
    name="xenforo_auth_provider",
    version="0.0.1",
    py_modules=['xenforo_auth_provider'],
    description="Xenforo provider for Synapse",
    include_package_data=True,
    zip_safe=True,
    install_requires=[],
)
